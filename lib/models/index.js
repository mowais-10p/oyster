// models will contain data dependent classes
var storeHouse = global.Packages.StoreHouse;

function initialize(params){
    return storeHouse.initialize(params);
}

module.exports.initialize = initialize;
